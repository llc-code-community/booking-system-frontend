import {styled} from 'styled-components';

export const TitleFavorite = styled.p`
    font-size: 22px;
    font-weight: 400;
`

export const QuantityFavorite = styled.span`
    font-size: 22px;
    font-weight: 400;
    opacity: 0.5;
`

export const SortFavorite = styled.button`
    font-family: "Helvetica Now Display";
    font-size: 14px;
    font-weight: 400;
`